一、Agent 架构设计与理解

基础理解
1. 请介绍一下你对 AI Agent 的理解，Agent 和普通的 LLM 调用有什么本质区别？
2. 你的项目中使用了 LangGraph 来编排多 Agent，为什么选择 LangGraph 而不是 LangChain 的 AgentExecutor 或者 CrewAI？它们之间有什么区别？
3. 解释一下 ReAct（Reasoning + Acting）模式的原理，你的项目中哪些 Agent 使用了 ReAct 模式，哪些没有？为什么要做这样的区分？
4. 你了解 Function Calling 和 ReAct 的区别吗？你的项目中工具调用是基于哪种方式实现的？各自的优缺点是什么？

架构设计
5. 你把系统拆分成了 6 个 Agent，拆分的依据是什么？如果让你重新设计，你会怎么拆分？有没有过度拆分或拆分不足的地方？
6. 你的三阶段流水线（并行搜索 → 按天规划 → 验证拼装）是怎么设计出来的？为什么不把所有事情放在一个阶段完成？
7. TripPlanState 作为全局状态在所有节点间流转，这种设计的优缺点是什么？如果状态字段越来越多，你怎么管理？
8. Phase2 中每天的景点、酒店、餐饮三个 Agent 是并行执行的，但酒店 Agent 依赖景点 Agent 的输出（需要景点位置摘要），你是怎么处理这个依赖关系的？
9. LangGraph 的 StateGraph 和普通的 DAG（有向无环图）有什么区别？LangGraph 的条件边（conditional_edges）本质上是什么？它和 if-else 有什么不同？

深入追问
10. 你的 should_continue_planning 条件边实现了按天循环，如果用户规划一个 7 天的行程，Phase2 需要串行执行 7 轮。你有没有考虑过让多天并行规划？这样做会有什么问题？
11. 如果让你给这个系统加一个"用户中途修改需求"的能力（比如规划到第 3 天时用户说"我不想去博物馆了"），你会怎么设计？
12. 你的 _run_async 函数中，当检测到已有事件循环在运行时，会创建新线程来执行协程。这种做法有什么问题？为什么不直接用 await？在 FastAPI 的异步上下文中，这种模式会导致什么风险？
13. 你的系统中所有 Agent 共享同一个 LLM 实例，在并行执行时会有什么并发风险？你是怎么考虑的？
14. 多个工具调用型 Agent 共享同一个 MCP 连接，会导致什么问题？你了解 MCP 连接池的概念吗？怎么设计？

二、Prompt 工程

15. 你提到使用了"角色定义 + 输出格式约束 + 业务规则 + 记忆注入"的四层 Prompt 结构，能具体解释每一层的作用吗？如果去掉其中一层会怎样？
16. 你是怎么让 LLM 稳定输出 JSON 格式的？除了在 Prompt 中要求，还有哪些技术手段？
17. 你的预算控制是通过在 Prompt 中注入具体金额上限来实现的（如"酒店不超过 600 元"），但 LLM 并不总是遵守。你怎么评估 Prompt 约束的有效性？如果 LLM 经常不遵守，你会怎么优化？
18. 你的 Prompt 目前是硬编码在代码中的，你认为这有什么问题？如果要做 Prompt 的版本管理和 A/B 测试，你会怎么设计？
19. 你的景点规划 Prompt 中注入了候选景点列表、天气数据、预算上限、用户偏好等多种信息，Prompt 的总 Token 数大概是多少？你有没有遇到过上下文窗口溢出的问题？怎么解决的？
20. 你的 System Prompt 和 User Prompt 是怎么分工的？为什么不把所有内容都放在 User Prompt 里？System Prompt 在多轮对话中有什么特殊作用？

三、工具调用与 MCP 协议

21. 解释一下 MCP（Model Context Protocol）协议是什么？它和直接调用 REST API 有什么区别？你的项目中是怎么通过 MCP 调用高德地图 API 的？
22. 你实现了工具调用的三层防护机制（tool_choice 策略切换、调用次数阈值、硬性迭代上限），能详细解释每一层的设计思路吗？为什么需要三层而不是一层？
23. 你提到"首轮强制调用工具（tool_choice="any"），后续轮次自动模式"，为什么第一轮要强制？如果 LLM 第一轮就能给出正确答案，强制调用工具不是浪费吗？
24. 你的 MCP 缓存层使用了 sort_keys=True 来保证参数顺序无关，但如果参数值本身是浮点数（如经纬度），精度差异会导致缓存未命中，你怎么处理？
25. 你的工具调用无限循环问题，文档中提到 LLM 会"用不同关键词反复搜索同一类信息"。除了你实现的三层防护，还有没有其他思路？比如能不能让 LLM 自己判断搜索结果是否足够？
26. MCP 协议相比传统的 REST API 封装，在 Agent 场景下有什么优势？如果高德地图没有提供 MCP 服务，你会怎么把 REST API 包装成 Agent 可用的工具？

四、记忆系统设计

27. 你实现了三层记忆架构（短期 State + 长期偏好 Redis + 长期情景 Milvus），能解释为什么需要三层？每一层解决什么问题？
28. 你的向量记忆使用 all-MiniLM-L6-v2 模型生成 384 维向量，为什么选择这个模型？它的优缺点是什么？如果要支持中文场景，你会换成什么模型？
29. Milvus 中使用 IVF_FLAT 索引 + COSINE 相似度，能解释 IVF_FLAT 的原理吗？和 HNSW 相比有什么优劣？在你的数据量级下，选择哪个更合适？
30. 你提到"行程摘要使用自然语言格式而非 JSON，提升语义检索的匹配质量"，能解释为什么自然语言格式更适合语义检索？
31. 新用户没有历史记忆（冷启动问题），你是怎么处理的？有什么改进思路？
32. 你的偏好画像使用 Redis Hash 存储，preferred_categories 是偏好频次统计。如果用户的偏好随时间变化（比如以前喜欢博物馆，现在喜欢户外），你的系统能捕捉到这种变化吗？
33. 你的偏好画像中 avg_daily_budget 使用滑动平均计算，这个算法的公式是什么？它和指数加权移动平均（EWMA）相比有什么区别？哪个更适合捕捉用户预算偏好的变化？
34. 你的记忆加载是在 node_load_memory 中一次性完成的，如果 Milvus 查询延迟很高（比如 2 秒），会阻塞整个规划流程。你有没有考虑过异步预加载或者缓存记忆？
35. 你的向量检索使用 filter 参数实现用户隔离，如果用户量达到百万级，每个用户有几十条记忆，Milvus 的 filter 性能会怎样？有没有更好的隔离方案？
36. 你的记忆存储节点 node_save_memory 如果执行失败（比如 Milvus 宕机），会影响用户拿到行程结果吗？你是怎么保证的？

五、LLM 输出解析与容错

37. 你实现了三层容错链（JSON 提取 → 字段级类型转换 → 节点级降级），能举一个实际遇到的 LLM 输出异常的例子，说明这三层是怎么协作的？
38. dict_to_attraction 中对每个字段独立 try-catch，如果一个景点的 name 字段解析失败了，你是丢弃整个景点还是用默认值？你的策略是什么？
39. 你的节点级降级策略是"景点解析失败 → 空列表"，这意味着用户可能看到某一天没有任何景点。你觉得这个降级策略合理吗？有没有更好的方案？
40. LLM 返回的 JSON 中，location 字段有时是 dict（{"longitude": 116.4, "latitude": 39.9}），有时是字符串（"116.4,39.9"），你是怎么兼容处理的？这种不一致的根因是什么？
41. 你有没有考虑过使用 Structured Output（如 OpenAI 的 response_format 参数或 Pydantic 的 JSON Schema）来强制 LLM 输出结构化数据？和你现在的方案相比有什么优劣？

六、后端架构与工程实践

FastAPI 与中间件
42. 你的中间件链是 RequestID → Auth → RateLimit → CORS，中间件的执行顺序为什么这样设计？如果把 RateLimit 放在 Auth 前面会怎样？
43. 你的滑动窗口限流算法是怎么实现的？和令牌桶、漏桶算法相比有什么优缺点？在分布式部署下，你的限流方案还能正常工作吗？
44. 你的熔断器实现了 closed → open → half-open 三态转换，能画出状态机图并解释每个状态的转换条件吗？
45. 你的熔断器使用 threading.Lock 保证线程安全，但 FastAPI 是基于 asyncio 的，Lock 在异步环境下会有什么问题？应该用什么替代？
46. FastAPI 的依赖注入（Depends）机制你是怎么使用的？和 Spring 的 IoC 容器相比有什么异同？
47. 你的 CORS 中间件配置了 CORS_ORIGINS，如果前端部署在多个域名下，你怎么动态管理允许的 Origin？

数据存储
48. 你用 Redis 作为主存储来存用户数据和行程数据，这个决策的考量是什么？你觉得有什么风险？如果要迁移到关系型数据库，你会怎么设计数据模型？
49. 你的 Redis 中行程数据是以什么格式存储的？如果要支持"按目的地查询所有行程"或"按日期范围查询"，你的存储结构能支持吗？
50. Redis 的持久化机制有 RDB 和 AOF 两种，你了解它们的区别吗？在你的场景下应该选择哪种？为什么？
51. 你的用户密码使用 Bcrypt 加密，rounds 设置为 12。能解释 Bcrypt 的工作原理吗？rounds 参数的含义是什么？设置为 12 是否合适？

并发与性能
52. 你的 Phase1 使用 asyncio.gather 并行执行景点搜索和天气查询，如果其中一个任务失败了，gather 的默认行为是什么？你是怎么处理的？
53. 你的系统在高并发场景下（比如 100 个用户同时发起行程规划），会有哪些瓶颈？LLM API 的并发限制、Redis 连接池、MCP 连接竞争，你分别是怎么考虑的？
54. 你提到 Phase2 是按天串行的（Day1 → Day2 → Day3），一个 3 天的行程大概需要多长时间？你做了哪些优化来缩短总耗时？
55. asyncio.gather 和 asyncio.wait 有什么区别？在什么场景下应该用 wait 而不是 gather？
56. 你的项目中使用了 ThreadPoolExecutor 和 asyncio 两种并发模型，能解释它们的区别吗？什么时候用线程池，什么时候用协程？

安全与认证
57. 你的 JWT 认证是怎么实现的？Token 存储在哪里？如何防止 Token 被盗用？你有实现 Token 刷新机制吗？
58. 你提到支持"访客模式"，访客和注册用户的权限区别是什么？访客的行程数据存在哪里？
59. 你的 JWT_SECRET 在配置中有默认值 "your-secret-key-change-in-production"，如果开发者忘记修改这个值就部署到生产环境，会有什么安全风险？你怎么防止这种情况？
60. 你的 API 有没有做输入校验来防止注入攻击？比如用户在 destination 字段输入恶意内容（Prompt 注入），会影响 LLM 的行为吗？

七、项目不足与优化

61. 你的项目文档中列出了 11 个已知问题，如果只能解决 3 个，你会优先解决哪 3 个？为什么？
62. 你提到 ContextManager 存在内存泄漏风险，能具体解释这个问题是怎么产生的？你会怎么修复？
63. 你的 Agent 间是单向流水线，缺乏反馈回路。如果 Phase3 验证发现某天景点全部被过滤了，当前系统会怎么处理？你理想中的反馈回路应该怎么设计？
64. 你的餐饮 Agent 是纯 LLM 推荐，没有调用工具搜索真实数据。这会导致什么问题？如果要改造成工具调用型，你预计会遇到什么挑战？
65. 你的前端进度条是模拟的，和后端实际进度不同步。如果要实现真实的进度推送，你会选择 SSE 还是 WebSocket？为什么？
66. 你的地理位置验证使用矩形边界盒（Bounding Box），这种方法有什么局限性？比如对于沿海城市或形状不规则的城市，会出现什么问题？有没有更精确的方案？
67. 你的系统目前没有对 LLM 的 Token 消耗做监控和控制。一次 3 天行程规划大概消耗多少 Token？如果要控制成本，你会怎么优化？
68. 你的 Haversine 公式用于计算景点间距离，这个公式的原理是什么？它和 Vincenty 公式相比精度如何？在你的场景下精度够用吗？

八、系统设计与扩展性

69. 如果要把这个系统从单机部署扩展到多机分布式部署，你需要改造哪些组件？
70. 如果要支持"多人协作编辑同一个行程"，你会怎么设计？需要考虑哪些并发冲突问题？
71. 如果 LLM 服务商（通义千问）突然不可用了，你的系统有没有降级方案？能否快速切换到其他 LLM？
72. 你的系统目前只支持国内城市，如果要扩展到国际城市，需要改造哪些模块？
73. 如果要给系统加上"行程对比"功能（同一目的地生成多个方案让用户选择），你会怎么设计？对现有架构有什么影响？
74. 如果要支持"增量修改行程"（比如用户说"把第二天的故宫换成颐和园"），你会怎么实现？是重新规划整天还是局部替换？各自的优缺点是什么？

九、代码质量与工程规范

75. 你的 agents.py 文件有 672 行，包含了状态定义、6 个节点函数、图构建、入口类。你觉得这个文件的职责是否过重？如果要重构，你会怎么拆分？
76. 你的项目中有两套目录结构（根目录的 app/ 和 backend/app/），这是怎么回事？哪个是实际使用的？
77. 你的异常处理策略是"捕获异常 → 记录日志 → 返回默认值"，这种策略在什么场景下会有问题？
78. 你的 Pydantic v2 模型中使用了 @model_validator 做日期校验，能解释 Pydantic v1 和 v2 的主要区别吗？为什么选择 v2？
79. 你的项目使用了 pyproject.toml 管理依赖，和 requirements.txt 相比有什么优势？你了解 uv 这个包管理工具吗？

十、LLM 基础知识

80. 你使用的是通义千问 qwen-plus 模型，能介绍一下这个模型的特点吗？它和 GPT-4、Claude 相比有什么优劣？
81. LLM 的 temperature 参数是什么含义？在你的场景中应该设置为多少？景点规划和主题生成应该用不同的 temperature 吗？
82. 什么是 Token？中文和英文的 Token 计算方式有什么区别？你的 Prompt 大概消耗多少 Token？
83. 什么是 LLM 的幻觉（Hallucination）？你的项目中遇到过哪些幻觉问题？你是怎么缓解的？
84. 什么是 RAG（Retrieval-Augmented Generation）？你的记忆系统算不算一种 RAG？和标准 RAG 有什么区别？

十一、Python 与异步编程

85. Python 的 GIL（全局解释器锁）是什么？它对你的多线程并行执行有什么影响？为什么你的 LLM API 调用用多线程仍然有效？
86. asyncio 的事件循环（Event Loop）是什么？为什么不能在一个已经运行的事件循环中再调用 asyncio.run()？你的 _run_async 是怎么解决这个问题的？
87. Python 的 TypedDict 和 dataclass 有什么区别？你为什么选择 TypedDict 来定义 TripPlanState？
88. 你的代码中使用了 list(state["assigned_attraction_names"]) 来做深拷贝，这种方式对嵌套对象有效吗？什么时候需要用 copy.deepcopy？

十二、综合开放题

89. 如果让你用一句话总结这个项目最大的技术挑战，你会怎么说？你是怎么解决的？
90. 回顾整个项目，你觉得最成功的设计决策是什么？最后悔的设计决策是什么？
91. 如果给你 3 个月时间重构这个项目，你会重点做哪些事情？
92. 你在开发这个项目的过程中，踩过最大的坑是什么？你从中学到了什么？
93. 如果面试官说"这个项目用一个 Prompt 就能搞定，为什么要搞这么复杂的多 Agent 架构？"，你怎么回应？
94. 你觉得 AI Agent 技术目前最大的瓶颈是什么？未来 1-2 年会有哪些突破？